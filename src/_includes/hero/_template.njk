<div class="bg-admin-second md:col-span-3 -mx-4 md:-mx-10 h-96">

<div class="lg:w-5/6 w-full ">
<h1 class="text-3xl md:text-4xl lg:text-5xl  font-extrabold text-white md:ml-12 p-8 lg:leading-normal md:leading-normal">{{ params.title }} </h1>
{%- if params.subtitle | length %}
<p class="text-white ml-8 md:ml-20 md:text-lg"> {{ params.subtitle }}</p>
{% else %} 
{% endif -%}
</div>

<div class="flex md:flex-row flex-col mt-6 md:mt-12 lg:mt-12 md:ml-12">
{%- if params.buttontext | length %}
<a href="{{ params.link }}" class="block w-max p-5 mx-8 my-2 md:mx-8 no-underline rounded hover:bg-gray-900 hover:text-underline text-white uppercase tracking-widest text-sm font-bold border border-solid border-white" aria-label="{{ params.arialabel }}"> {{ params.buttontext }} </a> 
{% else %} 
{% endif -%}

{%- if params.buttontext2 | length %}
<a href="{{ params.link2 }}" class="block w-max p-5 mx-8 my-2 md:mx-8 no-underline rounded hover:bg-gray-900 hover:text-underline text-white uppercase tracking-widest text-sm font-bold border border-solid border-white" aria-label="{{ params.arialabel2 }}"> {{ params.buttontext2 }} </a> 
{% else %} 
{% endif -%}
</div>

</div>

